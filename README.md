🚀 Order Execution Engine (Market Order + Mock DEX + WebSocket)

This project is a simple order execution engine that processes market orders, compares prices between Raydium and Meteora, and streams live status updates using WebSockets.
It is deployed on Render and works end-to-end with HTTP + WebSocket.
---------------------------------------------------------------------------------

🚀 Order Execution Engine (Market Order + Mock DEX + WebSocket)

This project is a simple order execution engine that processes market orders, compares prices between Raydium and Meteora, and streams live status updates using WebSockets.
It is deployed on Render and works end-to-end with HTTP + WebSocket.
------------------------------------------------------------------------------------------
